let num = [5, 8, 2, 9, 3];
num.sort(); // Ordena como strings por padrão (ex: 1, 2, 3, 5, 8, 9)
num.push(1); // Adiciona o número 1 ao final do array

console.log(num);
console.log(`O vetor tem ${num.length} posições`);
console.log(`O primeiro valor do vetor é ${num[0]}`);